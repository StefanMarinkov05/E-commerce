@model ArtFusionStudio.Models.ProductFeatures.PhoneAccessories.ProtectionLevel

@{
    ViewData["Title"] = "Редактирай";
}

<h1>Редактирай</h1>

<h4>Ниво на защита</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Protection" class="control-label">Ниво на защита</label>
                <input asp-for="Protection" class="form-control" />
                <span asp-validation-for="Protection" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="CaseTypes" asp-action="Index">Върни се назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
