@model ArtFusionStudio.Models.ProductFeatures.PhoneFeatures.OperatingSystemVersion

@{
    ViewData["Title"] = "Редактирай";
}

<h1>Редактирай</h1>

<h4>Версия на ОС</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="OSVersion" class="control-label">Версия на ОС</label>
                <input asp-for="OSVersion" class="form-control" />
                <span asp-validation-for="OSVersion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="OSNameAndVersions" asp-action="Index">Върни се към списъка</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
